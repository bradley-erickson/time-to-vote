.git
.venv
.claude
__pycache__
**/__pycache__

# All three are bind-mounted at runtime instead (see docker-compose.yml) —
# config/ and data/ hold real picks and your friends' names (both gitignored
# for the same reason), and static/images/contestants/ is generated by
# scripts/scrape_castaways.py rather than checked in.
config/
data/
static/
